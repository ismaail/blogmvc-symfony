doctrine:
    dbal:
        default_connection: default
        connections:
            default:
                driver: pdo_sqlite
                memory: true
    orm:
        metadata_cache_driver: array
        query_cache_driver: array
